GR00T-N1.7-3B (OXE_DROID embodiment)
Instruction: put small spoon from basket to tray

  critical   counterfactual.noun_swap                  score=0.1428
  critical   counterfactual.empty                      score=0.0664
  critical   counterfactual.ood_task                   score=0.0551
  critical   counterfactual.negation                   score=0.0575
  critical   counterfactual.lighting_shift             score=0.3717
  critical   counterfactual.gaussian_noise             score=0.2460
  pass       sweep.occlusion                           score=0.3862

=== Per-finding ===

[CRITICAL] language.noun_swap
  Action divergence under noun_swap averages 0.143, below the noise floor (0.5). The model produces nearly identical actions across variants — it isn't using the language.noun_swap cue.

[CRITICAL] language.empty
  Action divergence under empty averages 0.066, below the noise floor (0.5). The model produces nearly identical actions across variants — it isn't using the language.empty cue.

[CRITICAL] language.ood_task
  Action divergence under ood_task averages 0.055, below the noise floor (0.5). The model produces nearly identical actions across variants — it isn't using the language.ood_task cue.

[CRITICAL] language.negation
  Action divergence under negation averages 0.057, below the noise floor (0.5). The model produces nearly identical actions across variants — it isn't using the language.negation cue.

[PASS] vision.occlusion
  AUC over sweep = 0.386; robust along vision.occlusion.

[CRITICAL] vision.lighting
  Action divergence under lighting_shift averages 0.372, below the noise floor (0.5). The model produces nearly identical actions across variants — it isn't using the vision.lighting cue.

[CRITICAL] vision.sensor_noise
  Action divergence under gaussian_noise averages 0.246, below the noise floor (0.5). The model produces nearly identical actions across variants — it isn't using the vision.sensor_noise cue.
